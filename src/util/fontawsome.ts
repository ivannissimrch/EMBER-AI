import { config } from '@fortawesome/fontawesome-svg-core';
import '@fortawesome/fontawesome-svg-core/styles.css'; // Import CSS globally
config.autoAddCss = false; // Prevent Font Awesome from adding CSS automatically
